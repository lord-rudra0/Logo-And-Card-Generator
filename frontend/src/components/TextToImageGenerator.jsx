// TextToImageGenerator removed — ML-based image generation is disabled.
// Keep a small stub so imports don't break.
const TextToImageGenerator = () => {
  return (
    <div style={{ padding: 12, borderRadius: 8, background: 'var(--panel-muted)', color: 'var(--text-secondary)' }}>
      <strong>Text-to-Image</strong>
      <div style={{ marginTop: 8 }}>This feature has been removed from this build.</div>
    </div>
  )
}

export default TextToImageGenerator
